# Single source of truth for runtime deps — pyproject.toml reads this file
# (dynamic dependencies), so edit here only. Full-line comments only: setuptools
# does not strip inline "#" comments when parsing this file.
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
PyYAML>=6.0
pymupdf>=1.24
openai>=1.30
qdrant-client>=1.9
rank-bm25>=0.2
snowballstemmer>=2.2
# cross-encoder reranker (ONNX, CPU, no torch) — retrieval.rerank
flashrank>=0.2
# orchestration: ask pipeline as a graph (UPSC_RAG_PIPELINE=graph)
langgraph>=0.2
# message/runnable primitives used by the graph layer
langchain-core>=0.3
# ChatOpenAI for the tool-calling agent (UPSC_RAG_PIPELINE=agentic)
langchain-openai>=0.2
# DuckDuckGo web search (free, no key) — agentic pipeline's web_search tool
ddgs>=9.0
fastapi>=0.110
uvicorn[standard]>=0.29
# langfuse v3 dropped the .trace()/.generation() API used in observability.py
langfuse>=2.0,<3

# Dev (optional): pip install -r requirements-dev.txt
