# Core — also declared in pyproject.toml
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
PyYAML>=6.0
pymupdf>=1.24
openai>=1.30
qdrant-client>=1.9
rank-bm25>=0.2
snowballstemmer>=2.2
flashrank>=0.2   # cross-encoder reranker (ONNX, CPU, no torch) — retrieval.rerank
langgraph>=0.2          # orchestration: ask pipeline as a graph (UPSC_RAG_PIPELINE=graph)
langchain-core>=0.3     # message/runnable primitives used by the graph layer
langchain-openai>=0.2   # ChatOpenAI/OpenAIEmbeddings — optional portability seam (UPSC_RAG_LLM_BACKEND=langchain); also the tool-calling agent (UPSC_RAG_PIPELINE=agentic)
ddgs>=9.0               # DuckDuckGo web search (free, no key) — agentic pipeline's web_search tool
networkx>=3.0
fastapi>=0.110
uvicorn[standard]>=0.29
pyvis>=0.3
langfuse>=2.0,<3   # v3 dropped the .trace()/.generation() API used in observability.py

# Dev (optional): pip install -r requirements-dev.txt
